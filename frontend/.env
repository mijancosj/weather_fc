# Only needed for production builds where the backend isn't reachable at a
# relative /api path (dev uses the Vite proxy in vite.config.ts instead).
# Not sensitive — safe to commit as-is.
VITE_API_BASE_URL=http://localhost:8000
